type DomainRawTextFiles @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  description: String
  text: String
}

type Note @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  description: String
  image: String
}
 